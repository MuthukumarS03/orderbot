'use strict';


require(['config'], function (config) {

    require([
        'jquery',
        'router',
        'lib/shim/compat',
        'lib/bb-errors',
        'lib/auto-submit'
    ], function ($, Router) {

        var router = new Router();

    });

});
